url: "https://jasen.org.rs"
static_url: "https://storage.googleapis.com/jasen.org.rs"

# Build settings
markdown: kramdown
plugins:
- jekyll-coffeescript
- jekyll-feed
- jekyll-redirect-from
- jekyll-sitemap

sass:
  style: compressed

exclude:
- Gemfile
- Gemfile.lock
- node_modules/
- src/
- vendor/
